import { defineConfig } from 'astro/config';


// https://astro.build/config
export default defineConfig({
  // If hosting on a subdomina, set the base here such as /itsjpo, /demo, etc. 
  base: '',
  output: 'static',
});

