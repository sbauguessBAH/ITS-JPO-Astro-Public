---
import type { HTMLAttributes } from "astro/types";
import "./section.css";

type Props = HTMLAttributes<"p">;

const { class: className, ...rest } = Astro.props;
---

<p
  data-slot="ui_section-headline"
  class:list={["ui_section-headline", className]}
  {...rest}
>
  <slot />
</p>
