---
import type { HTMLAttributes } from "astro/types";
import "./section.css";

type Props = HTMLAttributes<"div">;

const { class: className, ...rest } = Astro.props;
---

<div
  data-slot="ui_section-icon"
  class:list={["ui_section-icon", className]}
  {...rest}
>
  <slot />
</div>
