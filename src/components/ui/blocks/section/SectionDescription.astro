---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"p">;

const { class: className, ...rest } = Astro.props;
---

<p
  class:list={["ui_section-description", className]}
  data-slot="ui_section-description"
  {...rest}
>
  <slot />
</p>
