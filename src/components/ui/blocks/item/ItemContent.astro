---
import type { HTMLAttributes } from "astro/types";

interface Props extends Omit<HTMLAttributes<"div">, "class"> {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<div
  class:list={["ui_item-content", className]}
  data-slot="ui_item-content"
  {...rest}
>
  <slot />
</div>
