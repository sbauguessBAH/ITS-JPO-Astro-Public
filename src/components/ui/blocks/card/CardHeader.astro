---
import type { HTMLAttributes } from "astro/types";

import "./card.css";

type Props = HTMLAttributes<"div">;

const { class: className, ...rest } = Astro.props;
---

<div
  class:list={["ui_card-header", className]}
  data-slot="ui_card-header"
  {...rest}
>
  <slot />
</div>
