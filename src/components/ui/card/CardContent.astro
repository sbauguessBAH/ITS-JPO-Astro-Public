---
import type { HTMLAttributes } from 'astro/types'
import './card.css';

type Props = HTMLAttributes<"div">;
const {
  class: className, ...props
} = Astro.props;
---
<div data-slot="program-card-content" class:list={["program-card-content", className]} {...props}>
  <slot/>
</div>