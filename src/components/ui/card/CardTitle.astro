---
import type { HTMLAttributes } from 'astro/types'
import './card.css';

type Props = HTMLAttributes<"div">;
const {
  class: className, ...props
} = Astro.props;
---
<div data-slot="program-card-title" class:list={["program-card-title", className]} {...props}>
  <slot/>
</div>