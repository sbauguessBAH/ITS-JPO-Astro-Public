---
import type { HTMLAttributes } from 'astro/types'
import './card.css';

type Props = HTMLAttributes<"div">;
const {
  class: className, ...props
} = Astro.props;
---
<div data-slot="program-card-action" class:list={["program-card-action", className]} {...props}>
  <slot/>
</div>