---
import type { HTMLAttributes } from 'astro/types'
import './card.css';

type Props = HTMLAttributes<"div">;
const {
  class: className,
  ...rest
} = Astro.props;
---
<div data-slot="program-card-header" class:list={["program-card-header", className]} {...rest}>
  <slot/>
</div>