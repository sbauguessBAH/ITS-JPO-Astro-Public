---
import { type ImageMetadata } from "astro";
import { Image } from "astro:assets";

interface Props {
  image?: ImageMetadata
  title: string
}

const { image, title } = Astro.props;
---

<div class="mb-4">
  <div class="heading">
    {image && (
      <div class="icon rounded-circle d-flex align-items-center justify-content-center">
        <img 
          src={image.src} 
          alt={`Icon for ${title}`} 
        />
      </div>
    )}
    <h2 class:list={[{"mb-2": !image}, {"mb-0": image}]}>{title}</h2>
  </div>
  <hr class:list={["heading-border", {"image-border": image}]}/>
</div>

<style>
.icon {
  background-color: rgb(3,48,68);
  height: 3rem;
  width: 3rem;
  margin-right: 0;
}

.icon img {
  height: 2rem;
  width: 2rem;
  object-fit: contain;
}

.heading {
  color: rgb(3,48,68);
  display: flex;
  align-items: center;
  gap: 0.4rem;
}

.heading-border {
  margin: -4px 0px 0px 0px;
  border-width: 4px;
  border-color: rgb(3,48,68);
  opacity: 1.0;
  height: 4px;
  border-radius: 2px;
}

.image-border {
  margin-left: 1.5rem;
}
</style>