---
import base_url from '@/src/lib/base_url';
import hero1 from "@/src/assets/images/Hero-Image-02.jpg";
import hero2 from "@/src/assets/images/Hero-ITS-JPO-NOW.png";
import hero3 from "@/src/assets/images/Hero-Background-4.png";
import hero4 from "@/src/assets/images/Hero-IntersectionSafety.png";
---

<div class="hero-carousel">
  <div class="slides">
    <!-- <section class="slide" style={`background-image: url(${hero1.src})`}>
      <div class="container-xl">
        <div class="content-box">
          <h2>Intelligent Transportation Systems Joint Program Office</h2>
          <p>
            The Intelligent Transportation Systems Joint Program Office (ITS JPO) seeks to transform the way society moves. Explore our website for information on research and deployments that advance
            the use of ITS to move people and goods more safely and efficiently.
          </p>
          <a href={base_url('/about/')} class="btn btn-outline-light btn-lg text-dark m-0 fs-4">LEARN MORE ABOUT THE ITS JPO</a>
        </div>
      </div>
    </section> -->

   <!-- 
    <section class="slide" style={`background-image: url(${hero3.src})`}>
      <div class="container-xl">
        <div class="content-box p-3">
          <iframe class="rounded-3 w-100" style="aspect-ratio: 16/9;" src="https://www.youtube.com/embed/XfNiDWilfss" title="What Is V2X?: A Short Dive into the Evolution and Future of Vehicle Connectivity" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>
        </div>
      </div>
    </section> -->

    <section class="slide" style={`background-image: url(${hero2.src})`}>
      <div class="container-xl">
        <div class="content-box">
          <h2 class="newsLetterHeader">ITS JPO NOW</h2>
          <p class="fw-bold">May 2026</p>
          <p>Georgia launches an accessible multimodal trip planner, USDOT unveils an interactive V2X deployment map, and Detroit hosts cutting-edge connected vehicle demonstrations—don't miss the May edition!

</p>
          <a href={base_url('/about/contact-us/mailinglist/ITS-JPO-NOW/may-2026/')} class="btn btn-outline-light btn-lg text-dark m-0 fs-4">Read Latest Issue</a>
        </div>
      </div>
    </section>


     <section class="slide" style={`background-image: url(${hero4.src})`}>
      <div class="container-xl">
        <div class="content-box">
          <h2>Check Out New Resources Related to Intersection Safety</h2>
          <p class="fs-6">
            Want to learn more about ITS for intersection safety and results from the USDOT ISC? Check out new resources now posted to the website, including two new reports related to the technical evaluation results and insights from the Challenge Stage 1B, a new video summarizing the results, and presentation materials from the webinar held on March 26.
          </p>
          <a href={base_url('/research-areas/intersection-safety/challenge/')} class="btn btn-outline-light btn-lg text-dark m-0 fs-5 mb-1">Explore the ISC Website</a>
          <a href="https://rosap.ntl.bts.gov/view/dot/89360" class="btn btn-outline-light btn-lg text-dark m-0 fs-5 mb-1" target="_blank">Challenge Stage 1B Lessons Learned Report</a>
          <a href="https://rosap.ntl.bts.gov/view/dot/89361" class="btn btn-outline-light btn-lg text-dark m-0 fs-5 mb-1" target="_blank">Challenge Stage 1B Lessons Learned Report</a>

        </div>
      </div>
    </section>

    <section class="slide" style={`background-image: url(${hero3.src})`}>
      <div class="container-xl">
        <div class="content-box">
          <h2 class="newsLetterHeader">ISS Prototyping Broad Agency Announcement is Live!</h2>
          <p class="fs-4">
            Funding Opportunity | Up to $17M across 3 teams
          </p>
          <a href="https://www.sam.gov" class="btn btn-outline-light btn-lg text-dark m-0 fs-5 mb-1">Learn More ▸</a>
          
        </div>
      </div>
    </section>


  </div>
</div>

<script>
  const carousel = document.querySelector('.hero-carousel .slides') as HTMLElement;
  const contentBoxes = document.querySelectorAll('.content-box');
  const slideWidth = carousel?.offsetWidth || 0;
  const totalSlides = 3;
  let currentSlide = 0;
  let autoRotateInterval: ReturnType<typeof setInterval>;

  function updateActiveSlide() {
    if (!carousel) return;
    const newSlide = Math.round(carousel.scrollLeft / carousel.offsetWidth);
    
    if (newSlide !== currentSlide) {
      // Remove active class from all
      contentBoxes.forEach(box => box.classList.remove('active'));
      
      // Add active class to current slide's content
      if (contentBoxes[newSlide]) {
        contentBoxes[newSlide].classList.add('active');
      }
      
      currentSlide = newSlide;
    }
  }

  function rotateToNextSlide() {
    if (!carousel) return;
    currentSlide = (currentSlide + 1) % totalSlides;
    carousel.scrollTo({
      left: currentSlide * carousel.offsetWidth,
      behavior: 'smooth'
    });
  }

  function startAutoRotate() {
    autoRotateInterval = setInterval(rotateToNextSlide, 10000);
  }

  function stopAutoRotate() {
    if (autoRotateInterval) {
      clearInterval(autoRotateInterval);
    }
  }

  // Initialize first slide as active
  if (contentBoxes[0]) {
    contentBoxes[0].classList.add('active');
  }

  // Start auto-rotation
  startAutoRotate();

  // Pause on hover
  carousel?.addEventListener('mouseenter', stopAutoRotate);
  carousel?.addEventListener('mouseleave', startAutoRotate);

  // Update active slide on scroll
  carousel?.addEventListener('scroll', updateActiveSlide);
</script>

<style>
  h1, h2, h3, h4, h5, h6 {
    color: white;
    text-wrap: balance;
  }
  section {
    margin: 0;
    padding: 0;
  }
  .hero-carousel {
    position: relative;
    min-height: 500px;
    isolation: isolate;
  }

  /* Scroll container with snap */
  .slides {
    display: flex;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    scrollbar-width: none;
    -webkit-overflow-scrolling: touch;
    height: 100%;
    scroll-marker-group: after;
  }

  .slides::-webkit-scrollbar {
    display: none;
  }

  /* Individual slides */
  .slide {
    flex: 0 0 100%;
    scroll-snap-align: start;
    scroll-snap-stop: always;
    min-height: 500px;
    background-size: cover;
    background-position: center;
    display: flex;
    align-items: center;
  }

  /* Scroll marker group positioning */
  .slides::scroll-marker-group {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 0.75rem;
    z-index: 10;
  }

  /* Individual scroll markers */
  .slide::scroll-marker {
    content: "";
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.6);
    outline: rgba(0, 0, 0, 0.5) solid 1px;
    cursor: pointer;
    transition: all 0.3s ease-in-out;
  }

  .slide::scroll-marker:hover {
    background: rgba(255, 255, 255, 0.8);
    transform: scale(1.2);
  }

  .slide::scroll-marker:focus,
  .slide::scroll-marker:target-current {
    outline: 2px solid white;
    outline-offset: 3px;
    background-color: #efbd27;
  }



  /* Scroll buttons */
  .slides::scroll-button(left),
  .slides::scroll-button(right) {
    content: '\f053';
    font-family: FontAwesome;
    font-size: 16px;
    position: absolute;
    bottom: 1.25rem;
    left: 50%;
    transform: translateX(calc(-50% - 80px));
    width: 32px;
    height: 32px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    z-index: 10;
    background-color: #efbd27;
    color: black;
    font-weight: 800;
    border-color: transparent;
  }

  .slides::scroll-button(right) {
    content: "\f054";
    left: 50%;
    transform: translateX(calc(-50% + 80px));
  }

  .slides::scroll-button(left):hover,
  .slides::scroll-button(right):hover {
    background-color: #66a3cc;
    color: white;
    border-color: transparent;
  }

  .slides::scroll-button(left):focus,
  .slides::scroll-button(right):focus {
      box-shadow: 0 0 0 0.2rem rgba(255, 255, 255, 0.25);
  }

  /* Content box */
  .content-box {
    background: rgba(7, 46, 85, 0.85);
    border-radius: 15px;
    padding: 2rem;
    max-width: 720px;
    color: white;
    margin: 1rem 0 5rem 0;
    opacity: 0;
    transform: translateX(100px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }

  .content-box.active {
    opacity: 1;
    transform: translateX(0);
  } 

  .content-box h2 {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .content-box h2.newsLetterHeader {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: #efbd27;
  }

  .content-box p {
    font-size: 1.25rem;
    margin-bottom: 1rem;
    line-height: 1.6;
    text-wrap: pretty;
  }

  /* Responsive */
  @media (max-width: 767px) {
    .hero-carousel {
      min-height: 400px;
    }

    .slide {
      min-height: 400px;
    }

    .content-box {
      padding: 1.25rem;
      margin: 1rem 0 5rem 0;
    }

    .content-box h2 {
      font-size: 1.5rem;
    }

    .content-box p {
      font-size: 1rem;
    }
  }
</style>