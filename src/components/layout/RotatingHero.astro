---
import base_url from "@/src/lib/base_url";
import hero1 from "@/src/assets/images/Hero-Image-02.jpg";
import hero2 from "@/src/assets/images/Hero-ITS-JPO-NOW.png";
import hero3 from "@/src/assets/images/Hero-Background-4.png";
import hero4 from "@/src/assets/images/Hero-WorldCup-Map.jpg";
import hero6 from "@/src/assets/images/Hero-stadium.jpg";

import hero5 from "@/src/assets/images/Hero-DOTBOTS.jpg";
---

<div class="hero-carousel">
  <div class="slides">
    <!-- <section class="slide" style={`background-image: url(${hero1.src})`}>
      <div class="container-xl">
        <div class="content-box">
          <h2>Intelligent Transportation Systems Joint Program Office</h2>
          <p>
            The Intelligent Transportation Systems Joint Program Office (ITS JPO) seeks to transform the way society moves. Explore our website for information on research and deployments that advance
            the use of ITS to move people and goods more safely and efficiently.
          </p>
          <a href={base_url('/about/')} class="btn btn-outline-light btn-lg text-dark m-0 fs-4">LEARN MORE ABOUT THE ITS JPO</a>
        </div>
      </div>
    </section> -->

    <!-- 
    <section class="slide" style={`background-image: url(${hero3.src})`}>
      <div class="container-xl">
        <div class="content-box p-3">
          <iframe class="rounded-3 w-100" style="aspect-ratio: 16/9;" src="https://www.youtube.com/embed/XfNiDWilfss" title="What Is V2X?: A Short Dive into the Evolution and Future of Vehicle Connectivity" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>
        </div>
      </div>
    </section> -->

    <!-- <section class="slide" style={`background-image: url(${hero2.src})`}>
      <div class="container-xl">
        <div class="content-box">
          <h2 class="newsLetterHeader">Engage with Us at ITSA 2026</h2>
          <p class="fs-4 mb-2"><em>Heading to ITSA 2026 in Detroit June 9-12?</em></p>
          <p class="fs-4">Visit the ITS JPO <strong>booth (#4031)</strong> in the exhibit hall to engage with our program managers and discuss our current AI and V2X technology initiatives!</p>
          <a
            href="https://www.itsamericaevents.com/expo/en-us/exhibitor-list/exhibitor-details.us%20department%20of%20transportation%20usdot.org-24c8ad9b-6cae-4e15-b39b-775ba369e9ae.html#/"
            class="btn btn-outline-light btn-lg text-dark m-0 fs-5 mb-1 icon-external-link"
            target="_blank"
          >
            Dive Deeper into ITSA 2026
          </a>
          <a href={base_url("/about/itsa-schedule/")} class="btn btn-outline-light btn-lg text-dark m-0 fs-5 mb-1 icon-arrow-button">View Our Activities Schedule</a>
        </div>
      </div>
    </section> -->

    <section class="slide" style={`background-image: url(${hero3.src})`}>
      <div class="container-xl">
        <div class="content-box">
          <h2 class="newsLetterHeader">ISS Prototyping Broad Agency Announcement is Live!</h2>
          <p class="fs-4">Funding Opportunity | Up to $17M across 3 teams</p>
          <a href="https://sam.gov/workspace/contract/opp/343dd0f244df4c3a9c9baa9b60b8ac54/view" class="btn btn-outline-light btn-lg text-dark m-0 fs-5 mb-1 icon-arrow-button">Learn More</a>
        </div>
      </div>
    </section>

    <section class="slide" style={`background-image: url(${hero2.src})`}>
      <div class="container-xl">
        <div class="content-box">
          <h2 class="newsLetterHeader">ITS JPO NOW</h2>
          <p class="fw-bold">July 2026</p>
          <p>Read highlights from the 2026 ITS America Conference & Expo, major announcements across V2X, ITS4US, and AI initiatives, and upcoming events including ITE Annual Meeting 2026.</p>
          <a href={base_url("/about/contact-us/mailinglist/ITS-JPO-NOW/july-2026/")} class="btn btn-outline-light btn-lg text-dark m-0 fs-4 icon-arrow-button">Read Latest Issue</a>
        </div>
      </div>
    </section>

    <section class="slide" style={`background-image: url(${hero6.src})`}>
      <div class="container-xl">
        <div class="content-box">
          <h2>ITS for Planned Special Events Executive Briefing & World Cup 2026</h2>
          <p class="fs-5">
            The ITS JPO’s Deployment Evaluation Program (DEP) released five new Executive Briefings synthesize findings from real-world deployments, highlighting documented benefits, costs, and
            lessons learned to help transportation agencies make informed investment and operational decisions.
          </p>
          <a
            href={base_url("/about/contact-us/mailinglist/ITS-JPO-NOW/july-2026/#its-for-planned-special-events-executive-briefing-world-cup-2026")}
            class="btn btn-outline-light btn-lg text-dark m-0 fs-5 mb-1 icon-arrow-button"
          >
            Read the article
          </a>
        </div>
      </div>
    </section>

    <section class="slide" style={`background-image: url(${hero4.src})`}>
      <div class="container-xl">
        <div class="content-box">
          <h2>FIFA AccessMap Multimodal Event Edition</h2>
          <p class="fs-5">
            The Taskar Center for Accessible Technology (TCAT) at the University of Washington (UW) partnered with the Seattle FIFA World Cup 2026 Local Organizing Committee to develop custom
            AccessMap features.
          </p>
          <a href={base_url("/about/contact-us/mailinglist/ITS-JPO-NOW/july-2026/#world-cup-accessmap")} class="btn btn-outline-light btn-lg text-dark m-0 fs-5 mb-1 icon-arrow-button">
            Read the article
          </a>
        </div>
      </div>
    </section>

    <section class="slide" style={`background-image: url(${hero5.src})`}>
      <div class="container-xl">
        <div class="content-box">
          <h2 class="newsLetterHeader">Ready, Set, Robot!</h2>
          <h2>Join the $1.5 million DOT Bots Challenge Now</h2>
          <p class="">
            The Challenge offers up to $1.5 million in prize funding for innovative ideas on how advanced robotics technologies can solve public sector transportation challenges, improve
            infrastructure, and keep travelers and workers safe.
          </p>
          <a href={base_url("/about/contact-us/mailinglist/ITS-JPO-NOW/july-2026/#join-the-dot-bots-challenge")} class="btn btn-outline-light btn-lg text-dark m-0 fs-5 mb-1 icon-arrow-button">
            More about the Challenge
          </a>
        </div>
      </div>
    </section>
  </div>
</div>

<script>
  const carousel = document.querySelector(".hero-carousel .slides") as HTMLElement;
  const contentBoxes = document.querySelectorAll(".content-box");
  const slideWidth = carousel?.offsetWidth || 0;
  const totalSlides = 4;
  let currentSlide = 0;
  let autoRotateInterval: ReturnType<typeof setInterval>;

  function updateActiveSlide() {
    if (!carousel) return;
    const newSlide = Math.round(carousel.scrollLeft / carousel.offsetWidth);

    if (newSlide !== currentSlide) {
      // Remove active class from all
      contentBoxes.forEach((box) => box.classList.remove("active"));

      // Add active class to current slide's content
      if (contentBoxes[newSlide]) {
        contentBoxes[newSlide].classList.add("active");
      }

      currentSlide = newSlide;
    }
  }

  function rotateToNextSlide() {
    if (!carousel) return;
    currentSlide = (currentSlide + 1) % totalSlides;
    carousel.scrollTo({
      left: currentSlide * carousel.offsetWidth,
      behavior: "smooth",
    });
  }

  function startAutoRotate() {
    autoRotateInterval = setInterval(rotateToNextSlide, 10000);
  }

  function stopAutoRotate() {
    if (autoRotateInterval) {
      clearInterval(autoRotateInterval);
    }
  }

  // Initialize first slide as active
  if (contentBoxes[0]) {
    contentBoxes[0].classList.add("active");
  }

  // Start auto-rotation
  startAutoRotate();

  // Pause on hover
  carousel?.addEventListener("mouseenter", stopAutoRotate);
  carousel?.addEventListener("mouseleave", startAutoRotate);

  // Update active slide on scroll
  carousel?.addEventListener("scroll", updateActiveSlide);
</script>

<style>
  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    color: white;
    text-wrap: balance;
  }
  section {
    margin: 0;
    padding: 0;
  }
  .hero-carousel {
    position: relative;
    min-height: 500px;
    isolation: isolate;
  }

  /* Scroll container with snap */
  .slides {
    display: flex;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    scrollbar-width: none;
    -webkit-overflow-scrolling: touch;
    height: 100%;
    scroll-marker-group: after;
  }

  .slides::-webkit-scrollbar {
    display: none;
  }

  /* Individual slides */
  .slide {
    flex: 0 0 100%;
    scroll-snap-align: start;
    scroll-snap-stop: always;
    min-height: 500px;
    background-size: cover;
    background-position: center;
    display: flex;
    align-items: center;
  }

  /* Scroll marker group positioning */
  .slides::scroll-marker-group {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 0.75rem;
    z-index: 10;
  }

  /* Individual scroll markers */
  .slide::scroll-marker {
    content: "";
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.6);
    outline: rgba(0, 0, 0, 0.5) solid 1px;
    cursor: pointer;
    transition: all 0.3s ease-in-out;
  }

  .slide::scroll-marker:hover {
    background: rgba(255, 255, 255, 0.8);
    transform: scale(1.2);
  }

  .slide::scroll-marker:focus,
  .slide::scroll-marker:target-current {
    outline: 2px solid white;
    outline-offset: 3px;
    background-color: #efbd27;
  }

  /* Scroll buttons */
  .slides::scroll-button(left),
  .slides::scroll-button(right) {
    content: "\f053";
    font-family: FontAwesome;
    font-size: 16px;
    position: absolute;
    bottom: 1.25rem;
    left: 50%;
    transform: translateX(calc(-50% - 80px));
    width: 32px;
    height: 32px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    z-index: 10;
    background-color: #efbd27;
    color: black;
    font-weight: 800;
    border-color: transparent;
  }

  .slides::scroll-button(right) {
    content: "\f054";
    left: 50%;
    transform: translateX(calc(-50% + 80px));
  }

  .slides::scroll-button(left):hover,
  .slides::scroll-button(right):hover {
    background-color: #66a3cc;
    color: white;
    border-color: transparent;
  }

  .slides::scroll-button(left):focus,
  .slides::scroll-button(right):focus {
    box-shadow: 0 0 0 0.2rem rgba(255, 255, 255, 0.25);
  }

  .slides::scroll-button(left):disabled,
  .slides::scroll-button(right):disabled {
    background-color: rgba(255, 255, 255, 0.5);
    color: rgba(0, 0, 0, 0.5);
    cursor: not-allowed;
    border-color: transparent;
  }

  /* Content box */
  .content-box {
    background: rgba(7, 46, 85, 0.85);
    border-radius: 15px;
    padding: 2rem;
    max-width: 720px;
    color: white;
    margin: 1rem 0 5rem 0;
    opacity: 0;
    transform: translateX(100px);
    transition:
      opacity 0.8s ease-out,
      transform 0.8s ease-out;
  }

  .content-box.active {
    opacity: 1;
    transform: translateX(0);
  }

  .content-box h2 {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .content-box h2.newsLetterHeader {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: #efbd27;
  }

  .content-box p {
    font-size: 1.25rem;
    margin-bottom: 1rem;
    line-height: 1.6;
    text-wrap: pretty;
  }

  /* Responsive */
  @media (max-width: 767px) {
    .hero-carousel {
      min-height: 400px;
    }

    .slide {
      min-height: 400px;
    }

    .content-box {
      padding: 1.25rem;
      margin: 1rem 0 5rem 0;
    }

    .content-box h2 {
      font-size: 1.5rem;
    }

    .content-box p {
      font-size: 1rem;
    }
  }

  .icon-arrow-button::after {
    content: "\f061";
    font-family: FontAwesome;
    margin-left: 0.5rem;
    display: inline-block;
  }

  .icon-external-link::after {
    content: "\f35d";
    font-family: FontAwesome;
    margin-left: 0.5rem;

    display: inline-block;
  }

  .icon-external-link:hover::after,
  .icon-arrow-button:hover::after {
    transform: translateX(5px);
    transition: transform 0.3s ease-in-out;
  }
</style>
